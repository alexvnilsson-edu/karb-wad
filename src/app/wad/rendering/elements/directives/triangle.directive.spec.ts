import { WadTriangleElementDirective } from './triangle.directive';

describe('WadTriangleElementDirective', () => {
  it('should create an instance', () => {
    const directive = new WadTriangleElementDirective();
    expect(directive).toBeTruthy();
  });
});
