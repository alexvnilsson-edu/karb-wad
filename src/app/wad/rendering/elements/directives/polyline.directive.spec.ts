import { WadPolylineElementDirective } from './polyline.directive';

describe('WadPolylineElementDirective', () => {
  it('should create an instance', () => {
    const directive = new WadPolylineElementDirective();
    expect(directive).toBeTruthy();
  });
});
