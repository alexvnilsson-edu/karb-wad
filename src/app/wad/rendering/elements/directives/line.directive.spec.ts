import { WadLineElementDirective } from './line.directive';

describe('WadLineElementDirective', () => {
  it('should create an instance', () => {
    const directive = new WadLineElementDirective();
    expect(directive).toBeTruthy();
  });
});
