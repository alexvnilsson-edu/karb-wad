import { WadElementBaseDirective } from './base.directive';

describe('WadElementBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new WadElementBaseDirective();
    expect(directive).toBeTruthy();
  });
});
