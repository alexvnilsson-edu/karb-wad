import { WadRectangleElementDirective } from './rectangle.directive';

describe('WadRectangleElementDirective', () => {
  it('should create an instance', () => {
    const directive = new WadRectangleElementDirective();
    expect(directive).toBeTruthy();
  });
});
